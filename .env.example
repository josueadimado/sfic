DEBUG=True
DJANGO_SECRET_KEY=replace-with-a-secure-key
ALLOWED_HOSTS=127.0.0.1,localhost
# Public site URL (no trailing slash). In production use the host that works in the browser.
# For setfreeinchristmission.org, settings.py rewrites bare apex → www for emails and Stripe redirects.
# You can still set SITE_BASE_URL=https://www.setfreeinchristmission.org explicitly.
SITE_BASE_URL=http://127.0.0.1:8000
# Optional extra apex domains (comma-separated) that should always use www in outbound URLs:
# SITE_USE_WWW_FOR_HOST=example.org

# Minutes of no activity before the user must sign in again (dashboard + participant portal).
SESSION_IDLE_TIMEOUT_MINUTES=15
# Use True on production HTTPS so session cookies are only sent over secure connections.
# SESSION_COOKIE_SECURE=False

STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@freedomintensive.local
